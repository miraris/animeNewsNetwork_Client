{
  "name": "ann_client",
  "version": "1.0.0",
  "description": "a javascript client for anime news network",
  "main": "index.js",
  "scripts": {
    "clean": "rm ./index.js --force && find ./test -name '*.js*' -exec rm {} \\;",
    "pretest": "npm run clean && tsc -p ./",
    "test": "./node_modules/.bin/mocha --timeout 0 --ui bdd ./test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "rxjs": "^5.5.6"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.44",
    "@types/node": "^8.5.10",
    "@types/request": "^2.0.7",
    "@types/rx": "^4.1.1",
    "mkdirp": "^0.5.1",
    "mocha": "^4.0.1",
    "rewiremock": "^3.0.0",
    "ts-node": "^3.3.0",
    "typescript": "^2.5.3"
  }
}
